import { CustomBannerDirective } from './custom-banner.directive';

describe('CustomBannerDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomBannerDirective();
    expect(directive).toBeTruthy();
  });
});
